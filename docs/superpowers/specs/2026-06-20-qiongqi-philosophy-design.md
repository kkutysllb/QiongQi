# Qiongqi 叙事改造：穷奇设计哲学落库

> **状态**：草案，待用户 review
> **范围**：仅文档（一个 README 段落替换 + 一个新 DESIGN.md）
> **不动**：任何 `.ts` / 测试 / 配置 / 脚本 / git 仓库初始化

---

## 一、背景与目标

`qiongqi/README.md` 第 9-16 行将项目叙事锚定在《庄子·逍遥游》中的
"鲲"，定位为"提升每 token ROI 的本地运行时"。本轮由用户提供了一
套全新的叙事——以《山海经》《神异经》《左传》中的"穷奇"为原型，
把引擎的设计哲学概括为三条主张：

1. **核心哲学：打破"中央集权"的叛逆者**（对应去中心化）
2. **个体能力：独当一面的"虎翼"**（对应端到端独立性）
3. **群体智能：懂"人话"的自协商组织**（对应组合协作与解耦）

本 spec 落实这次叙事替换。

---

## 二、用户已锁定的决策（不再回头讨论）

| 决策点 | 取值 |
| --- | --- |
| 落地形式 | README 短版 + 新建 `qiongqi/DESIGN.md` 完整版 |
| 典故处理 | 完全替换：删除《庄子·逍遥游》/鲲，改为三典出处 |
| 引文风格 | 完整保留《左传》《山海经·海内北经》《神异经》原文与出处 |
| DESIGN 子标题 | 完全采用用户原话（含括号技术词） |
| 映射自评 | 写中性映射表（不掩盖与现状的真实差距） |
| 差距待办 | DESIGN.md 预留"叙事→实现差距待办"章节 |
| Git 初始化 | **不**初始化 git；spec 本地存、不 commit |
| 代码改动 | **不**改任何源码/测试/配置/脚本 |

---

## 三、改动清单

### 3.1 修改 `qiongqi/README.md`

仅替换第 9-16 行（8 行）。其余内容（第 1-7 行定位、第 18 行起的目
录结构/CLI/HTTP API/配置/故障排查/GUI 集成）一字不动。

#### 改动前（第 9-16 行原貌）

```markdown
Qiongqi 取意于《庄子·逍遥游》中的"北冥有鱼，其名为鲲"。在 KCoder
里，它代表一个更深的本地运行时：不是把模型回复包一层 UI，而是让模型可以
长期携带项目上下文、稳定调用工具、恢复会话，并在桌面、写作、手机连接和
定时任务之间复用同一套 agent loop。

Qiongqi 的核心目标是提高每一个 token 的 ROI。它会尽量让 token 花在用户需求、
代码、决策和结果上，而不是浪费在重复工具 schema、失控工具输出、畸形历史、
无效重试或本可以命中的稳定前缀上。
```

#### 改动后

```markdown
> **穷奇非凶，乃破局之锐。** Qiongqi 取意于《山海经》《神异经》《左传》
> 中的"穷奇"——一头"状如虎，有翼"、"知人言语"、敢"毁信废忠"的凶兽。
> 引擎拒绝中央调度寡头，每个 Agent 都是忠于任务的少昊氏不才子；
> 单兵可独立闭环作战，多个 Agent 则在解耦的引擎骨架与外部技能血肉之间
> 自协商达成共识。完整设计哲学见 [`DESIGN.md`](./DESIGN.md)。

Qiongqi 的核心目标是让 Agent 如凶兽般凶猛进化、在混沌中以去中心化的
方式达成共识，并以每一 token 的 ROI 为底盘：避免重复工具 schema、
失控工具输出、畸形历史、无效重试，以及任何可以命中却错过的稳定前缀。
```

### 3.2 新建 `qiongqi/DESIGN.md`

- **位置**：`qiongqi/DESIGN.md`（与 `README.md` 同级，不进顶层 `docs/`）
- **预估行数**：约 110 行
- **结构**：
  1. 顶部题记（一句口号）
  2. 文档说明（介绍本文档目的）
  3. **一、神话原型**：三个子节，每节包含"原文出处"+"用户提供的解读"，
     子标题完全采用用户原话（含括号技术词）
  4. **二、主张 ↔ 当前架构映射（自评）**：三行表格，分别给出
     ①部分吻合 / ②高度吻合 / ③完全吻合 的中性自评
  5. **三、叙事 → 实现差距待办**：三条可选重构 backlog
     （去中心化的回合编排 / 跨进程 Agent 寻址 / 议事规则的边界），
     并明确声明"本文档不强制任何具体实现路径"

---

## 四、验收口径

1. `qiongqi/README.md` 第 9-16 行被替换为 §3.1 中的新内容；其它行字节不变
2. 新建 `qiongqi/DESIGN.md` 存在，含 §3.2 描述的四个一级标题与三档自评
3. 改动完成后**不**执行：`git init`、`git add`、`git commit`、
   `npm run typecheck`、`npm run test`、`npm run build`
4. 改动完成后报告：
   - 改动的文件清单（2 个）
   - README 第 9-16 行 before/after diff 概要
   - DESIGN.md 的总行数
   - 一句话声明本次未触碰源码/未初始化 git

---

## 五、风险与回滚

| 风险 | 等级 | 缓解 |
| --- | --- | --- |
| GitHub markdown 渲染对 `>` 嵌套引用块的兼容性 | 低 | 若渲染异常，降级为单层 `>` 块 |
| 用户 review 时希望微调措辞 | 低 | README 改动集中在一段，DESIGN.md 结构清楚，回滚成本可控 |
| 主张①"去中心化"与现状真实冲突 | 中（但**仅叙事层面**） | 中性映射表显式标注 "部分吻合"；差距待办章节明确"不在本次实施" |
| SPEC/Plan 双层流程显得过重 | 低 | 本次纯文档，用户已在 brainstorming 内逐段确认；无 writing-plans 必要 |

### 回滚方式

- 恢复 `qiongqi/README.md` 第 9-16 行原文
- 删除 `qiongqi/DESIGN.md`
- 删除 `docs/superpowers/specs/2026-06-20-qiongqi-philosophy-design.md`

---

## 六、不做的事（YAGNI 显式清单）

- 不动任何 `.ts` 源码 / `.test.ts` 测试
- 不动 `package.json` / `tsconfig.json` / `tsconfig.build.json` /
  `vitest.config.ts` / `config.example.json` / `scripts/` 任何内容
- 不创建顶层 `docs/` 目录（DESIGN.md 放在 `qiongqi/` 内）
- 不引用 `assets/qiongqi.png`，不动 README 顶部 logo/banner
- 不动 `dist/` 产物（已 gitignore）
- 不初始化 git 仓库、不提交任何内容
- 不跑 typecheck / test / build
- 不引入新的依赖、不修改版本号
- 不重命名 Qiongqi 本身（项目仍叫 Qiongqi，只是取意典故换了）